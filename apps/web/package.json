{
  "name": "@gatherthread/web",
  "version": "0.1.0-alpha.6",
  "license": "Apache-2.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node scripts/serve.mjs",
    "build": "node scripts/build.mjs",
    "test": "node --test",
    "test:output": "node --test scripts/check-build-output.mjs",
    "check": "npm run test && npm run build && npm run test:output"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "katex": "^0.16.47",
    "micromark": "^4.0.2",
    "micromark-extension-gfm": "^3.0.0"
  },
  "devDependencies": {
    "esbuild": "^0.28.2"
  }
}
